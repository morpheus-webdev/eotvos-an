/* import express from 'express'

const app = express()
app.use(express.json())

app.get('/api-path', async (req,res) => {
    res.send({message: "Hello World!"})
})

app.listen(8000, () => {
    console.log('A szerver elindult a 8000-es porton')
}) */

//node pelda-server.js